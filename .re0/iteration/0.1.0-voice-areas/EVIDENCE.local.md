# EVIDENCE — 0.1.0 voice-areas

Gate rows for rounds 2–3. Statuses read back from `test-results/last-run.txt` (**23 passed, retries: 0**,
`/health` answered `{ok:true, model:large-v3-turbo, warm:true}` before the run), the unit run (**109/109**, was
88), the two round-2 review lenses and the round-2/3 fixes. Every path below was confirmed present on disk at
memo time; the unit counts were re-run at memo time (`npm test` → `Tests 109 passed (109)`). Caveats live in
RETRO.local.md (L1–L9); a row's caveat is the product behaviour the gate did not reach, not a hedge.

Provenance note for N8: the round-3 working tree was swept into an unrelated commit (`cd5e1cb`, kiosk acoustic
probe) by another process mid-round. Nothing in the evidence below depends on that commit — the cited proof is
the on-disk tree plus the re-driven runs — but a round-3-only diff has to be split out of `cd5e1cb`.

| Gate | Proof required | Status | Proof path / note |
| --- | --- | --- | --- |
| G1 vertical slice | real WAV → real STT → text inside the recognised shape | **met** | `test-results/evidence/g1-vertical-slice.png`; jfk.wav from the arm, stroke → ellipse, bbox unchanged within 2 scene px, transcript bound to the container. Round-1 looping-fixture caveat gone (clip played once). |
| G2 parallelism | 3 strokes in one hold, 3 transcripts, no stroke waits | **met (scope changed)** | `evidence/g2-parallelism.png`; three containers non-empty, `orphans: 0`, 3 shapes coexist while still holding with an earlier one already committed. Round-1's `maxPendingSeen >= 2` is **no longer asserted**: an utterance is dispatched only once VAD silence closes it, so two in-flight requests need two utterances ending inside one STT round trip, which no real-speech fixture produces (measured `maxPendingSeen: 1`). Accepted change of meaning, not a regression. |
| G3 recognition | unit + in-bundle geometry cases | **met** | `test/unit/stroke.test.ts` 11/11; shipped-bundle repeat at `evidence/g3-recognition.png` (circle→ellipse, box→rectangle, straight→line, near-vertical→rectangle, tap→null). |
| G4 fit | container dims unchanged for short/long/Korean; line text along the line | **met** | `evidence/g4a-fit-direct.png` (no growth at 240x120 **and** at the hostile 120x80 with a long Korean sentence — round 1's open half), `g4b-line-horizontal.png`, `g4c-line-slanted.png`, `g4d-line-legibility.png` (120 px line: fontSize ≥ 14, width ≤ 122, wrapped, text above the line). Documented exception stands: a shape too small for the floor font size is grown visibly. Harness-bent (L4): G4d uses the three-utterance fixture because the VAD seeds its floor on ko-long.wav's first syllable. |
| G5 failure paths | dead STT → ⚠ + retry; silence → shape kept; delete-while-pending | **met** | `evidence/g5a-failure.png`, `g5a-failure-retry.png`, `g5b-silence.png`, `g5c-delete-pending.png`. Harness-bent (L4): G5c uses the three-utterance fixture so no earlier utterance becomes an orphan free text. |
| G6 continuity | vanilla localStorage keys survive boot/reload/commit | **met** | `evidence/g6-continuity.png`; seeded `excalidraw` / `excalidraw-state` survive boot, reload, a voice commit and a second reload; ids, text and binding intact. |
| G7 kiosk | **round 3 live since 2026-09-18 00:5x KST** (`scripts/deploy.sh`, launcher with mic auto-grant + CDP port). Probe `/tmp/kiosk-r3.png`: page loads, `toolbar-voice` present, `capture.mic = ok` at boot, inputs Default + TouchDevice Mono, STT /health ok from the whiteboard; an armed stroke over CDP → ellipse 300×160 placeholder → no utterance in the silent room → discarded to a solid shape, no page errors (`scripts/kiosk-probe.mjs --stroke`). Round-1 evidence (22:37) superseded. | met (kiosk path) — **real-speech round trip on the wall still untested: the panel's mic array records digital silence, see REF-kiosk-audio.local.md** |
| G8 docs | CLAUDE.md + README cold-read verdict | **cold read: minor-gaps** | Reader reconstructed purpose, deploy, test and STT topology correctly. Four stalls, all additive: mic-button location has no visual description; no deploy-failure/rollback path for the single physical panel; unclear whether `npm run e2e` exit code is trustworthy when STT is down; stroke.ts geometry thresholds restated as fact in README with no pointer to the source of truth. |
| N2 boundary-race | zero-gap pair, disarm in window, undo while armed | **met (with an open sibling defect)** | `evidence/n2a-zero-gap.png`, `n2b-disarm-in-window.png`, `n2c-undo-while-armed.png`. Harness-bent (L4): N2a pins `preRollMs: 0`; at the default the assignment rule gives both sentences to the second stroke, and the only shape that can hold the first is a stroke spanning the deadline — which is the defect in the N2e row below. |
| N2d undo-before-speech | undone shape must not swallow the transcript | **met** | `test/e2e/voice.spec.ts:897`, `evidence/n2d-undo-before-speech.png`. Fails on the pre-fix `dispatch()` (transcript vanishes silently); passes with `liveTarget()`/`forgetStroke` — `orphans: 1`, `completed: 1`, `containerId: null`. |
| N2e pre-roll across a live stroke | default preRoll, speech then a slow stroke → orphans 0 | **unmet — open defect** | Measured on the real surface: utterance onset 639 ms (deadline 2139), pointer-down 1059, pointer-up 3025 → `orphans: 1`, "회의 안건 정리" placed as free text instead of in the container being drawn for it. Cause: a stroke enters `session.strokes` only inside the pointer-up conversion (`src/controller.ts:727`), so assignment finalises against a list missing the in-progress stroke. Fix shape: treat a live `currentStroke` like `Session.conversions` — provisional record or suppressed finality, re-run on push. Deliberately not asserted in the green suite. |
| N3 visible failure | denied/missing mic refuses to arm and says so | **met** | `evidence/n3-mic-denied.png`; mode stays `idle`, `recording: false`, typed `lastError`, `voice-tool--mic-missing`, activeTool unchanged, a later stroke produces no container. Deviation: headless Chromium rejects an ungranted `getUserMedia` with `NotSupportedError`, so the case asserts MicState ∈ {denied, missing, error}. |
| N5 zoom invariance | same physical gesture recognises the same at 0.5 / 1 / 2 | **met** | `evidence/n5-zoom-0.5.png`, `n5-zoom-2.png`, `n5-zoom.png`; 300x160 screen px → 600x320 scene px at 0.5 and 150x80 at 2, x within 2 scene px; a 6-screen-px tap creates nothing at either zoom. Closes round-1 L4. |
| N6 utterance assignment by words | three-utterance fixture, words per shape, both rhythms + palm tap | **met** | `evidence/n6a-draw-then-speak.png`, `n6b-speak-then-draw.png`, `n6c-palm-tap.png`; each shape holds its own words and is asserted NOT to hold its neighbours'; `orphans: 0`; the 3 px tap left no ink. Harness-bent (L4): `warmMicOnBoot: false` forced in every case, so the whole suite runs a cold mic the kiosk never has. |
| N7 single contract | src/ imports every contract it contains | **met** | `src/audio.ts` deleted, the retired recorder block removed from `contracts.ts`, `contracts-capture.ts` live and imported; `npm run build` green, no unreferenced module in `src/`. |
| N8 evidence integrity | retries 0, log at the cited path, every path exists | **met** | `playwright.config.ts:17` `retries: 0`; `outputDir: test-results/artifacts` so the start-of-run wipe spares the log; `test-results/last-run.txt` ends `23 passed (2.8m)` after the round-3 re-drive (was 2.7m in round 2); all cited evidence paths verified present at memo time, and `npm test` re-run to confirm 109/109. Closes round-1 L6. See the provenance note at the top about commit `cd5e1cb`. |
| R3 ghost sweep | a placeholder left by a reload is swept back to a plain shape | **met** | `evidence/r3-ghost-sweep.png`; a dashed rectangle with a bound "··" seeded into the vanilla `excalidraw` key returns solid, unbound, geometry unchanged, the "··" text gone. Unit-gated too: `test/unit/persist.test.ts` (12 cases). |

## Post-review fix gates (new unit rows — RETRO L5)

| Gate | Proof required | Status | Proof path / note |
| --- | --- | --- | --- |
| U1 supersede predicate | a target with no parts and an open utterance is not discarded | **met** | `test/unit/controller.test.ts` — the lens's exact case, a 60-combination property test asserting `isSuperseded === (assignUtterance picks the later stroke)`, and a full-controller case. Reverting the operator fails 3. This is the blocker the 22/22 suite could not see (RETRO L1). |
| U2 mic recovery after a latched failure | mute→error→prepare returns to ok without a reload | **met (node fake only)** | `test/unit/capture.test.ts`; `prepare()`'s fast path now gated on `mic === "ok"`, an intact graph re-validated or rebuilt. Fails on the pre-fix line. No browser or kiosk evidence — see U5. |
| U3 slow AudioContext resume | a context resuming at 900 ms stays ok, no spurious events | **met (node fake only)** | `test/unit/capture.test.ts`; one-shot 500 ms verdict replaced by a 250 ms poll up to 3 s. A context that never resumes still errors once with the same detail. |
| U4 STT upload filename | a wav blob uploads as .wav | **met** | `test/unit/stt.test.ts` stubs fetch and reads the FormData file name; wav/ogg/mp3/m4a mapped, else webm. Fails on the pre-fix branch (every round-2 upload was a WAV named .webm). |
| U5 hallucination short-entry | real short words survive the filter | **met** | `test/unit/hallucination.test.ts`; substring arm restricted to blocklist entries ≥ 8 chars — "young/youth/your/yours/payout/뉴스룸/속보 뉴스" survive (7 assertions fail on the pre-fix predicate) while every blocklist entry and "um thank you" still drop. |

## Round-3 gate rows (N10, N11, N12 — closed; RETRO "Closed / Retired by round 3")

| Gate | Proof required | Status | Proof path / note |
| --- | --- | --- | --- |
| N10 Status rendering | every `VoiceStatus` field is rendered and asserted, or carries a debug-only row | **met** | A drop now renders at the moment it happens: `controller.ts` raises `api.setToast({message, duration: 2500})` — `Filtered: "<text>"` for a blocklist/non-empty drop, `No speech heard for that shape` when a non-orphan target is discarded with no text — and `buttonTitle(status)` in `toolbar.tsx` adds `dropped N: "<last>"` to the mic button's tooltip. Proof: `test/unit/controller.test.ts` → describe "a dropped transcript is rendered, not only counted (gate N10)" → "toasts the filtered text so a blocklist hit cannot be mistaken for a silent room" + "toasts a shape that ends with no text at all" (both assert the 2500 ms duration); `test/unit/toolbar.test.ts` → describe "buttonTitle — the toolbar says how many transcripts were thrown away" → "says nothing about drops while nothing has been dropped", "names the count and the text that was filtered", "keeps the failure line as well: two different channels, both readable"; real surface `test/e2e/voice.spec.ts:462` (G5b) asserts `.Toast .Toast__message` reads "No speech heard for that shape", logged green at `test-results/last-run.txt:12` (test 9, 3.7s). Residue (accepted, owner: main loop, re-decide round 4): no e2e drives `status.dropped > 0` — the silence fixture never returns a transcript, so only a real hallucination could raise the counter; the tooltip half is gated by the pure `buttonTitle` instead, which is the N9 shape. |
| N11 Recovery | every blocking module state is entered, cleared, and the product works again without a reload | **met (node fake; kiosk still owes N13)** | Proof: `test/unit/capture.test.ts` → describe "N11(a) — an audio context that stays suspended past the window and then comes back" → "goes error, then ok, and arms again without a reload" (context suspended past the whole 3 s window → mic `error`; HAL returns → `stop()`/`start()` gives mic `ok`, `active: true`, `rig.calls === 1`, i.e. no re-acquire and no reload); describe "N11(b) — a muted track" → "errors on mute, clears on unmute, and starts again" (typed detail on the error, `start()` works on the same stream); the two `prepare()` cases renamed "N11(c): re-checks an intact graph instead of returning the latched error state" and "N11(c): tears the cached graph down and rebuilds when the context is no longer running"; plus the two round-2 slow-resume cases now titled N11(a). Every case names its gate in its title, so the gate is greppable from the suite. Caveat: node fake HAL only — the real audio graph is still N13. |
| N12 Scale agreement | the level meter and the VAD threshold marker are asserted to share one scale | **met** | `LEVEL_GAIN` (×4) deleted: `capture.onLevel` and the new `capture.noiseFloor` emit RAW RMS, with unit **and** scale stated in `src/contracts-capture.ts` (0..1, speech ~0.02–0.2); `src/level.ts` is the one display mapping (`meterPercent` at full scale 0.06, `effectiveThreshold(setting, 3× floor)`, `meterScale(rms, threshold, floor) → {bar, mark, threshold}`); `settings-panel.tsx` draws bar and marker from `meterScale`, `toolbar.tsx` routes `status.level` through `meterPercent` into the `--voice-level` CSS var, `vad.ts` exports `FLOOR_MULTIPLIER` and a readonly `noiseFloor`. Proof: `test/unit/level.test.ts` → describe "meterScale — bar and marker are the same function of the same unit" → "puts the marker exactly where the bar ends when the room sits on the threshold", "orders bar against marker the way the VAD orders level against threshold", "draws the marker at the threshold the VAD really uses, not at the setting alone", "agrees with vad.ts on where the line is, for the same floor" (property case over a live `createVad` across 5 room floors), "clamps into the meter instead of overflowing it"; `test/unit/capture.test.ts` → describe "N12 — the number the meter is drawn from" → "emits RAW RMS through onLevel, not a display-gained copy of it" + "reports the VAD's noise floor on that same scale". Before/after reproduced: re-inserting the ×4 fails the two capture cases (0.4 vs 0.1). Accepted (owner: main loop, re-decide round 4): the toolbar has no meter surface, so its half of the agreement is the CSS var + recording-dot halo, not a bar a test can read. |
| U4bis STT upload filename | a WAV blob uploads as `.wav` | **met (already covered)** | `test/unit/stt.test.ts` → "names the round-2 PCM blob segment.wav" asserts the multipart file name for an `audio/wav` blob plus the ogg/webm fallbacks. Re-run at memo time; no new test was needed. |

## Open rows — defects and untested risks (RETRO L5: every risk exits as a row)

| Item | Status | Note |
| --- | --- | --- |
| Level scale mismatch | **met (N12, round 3)** | See the N12 row below. |
| `dropped` / `lastDropped` not rendered | **met (N10, round 3)** | See the N10 row below. |
| Capture path on real hardware | **untested — gate N13, owner: main loop** | Worklet path, ScriptProcessor fallback, resume-after-gesture, clock-offset drift, WAV cut accuracy: offline arithmetic plus Chromium's fake device only. Cold-start suspend probe on the panel unrun (RETRO L8, gate N13). |
| `utteranceSession` never deleted from | **unmet — owner: main loop** | Every utterance the page hears keeps a strong reference to its Session, so `sessions.delete(owner)` frees nothing on a kiosk that never reloads (RETRO L6). Round 3 closed the sibling case for `FailedEntry` with `pruneFailed()`; copy that shape — name the deleter and prove the entry is gone after its key dies. Re-labelled from *untested* per L9: a map with a known missing deleter is a defect, not an unknown. |
| `FailedEntry` for a deleted shape | **met (round 3)** | `pruneFailed()` drops every failed entry whose container/text is gone from the scene (`findPair` miss) and forgets its stroke record; called at the top of `resolveTargets` and first in `retryFailed`, so the failed count, the lit retry button and the retained WAVs cannot grow. Proof: `test/unit/controller.test.ts` → "a failed entry whose shape has left the scene is pruned" (undo-then-disarm clears `failed` to 0; an attempt-exhausted entry that `retryFailed` used to skip forever is dropped on the next retry with no request sent). Both cases fail on the pre-fix controller. |
| Multi-touch second contact | **accepted, not tested — owner: main loop, re-decide round 4 (N14)** | A second pointer-down before the first pointer-up overwrites `currentStroke`; the first contact's ink is never converted. IR frame behaviour unknown until the kiosk pass. |
| `fit.discard` / `markFailed` restore | **accepted, not tested — owner: main loop, re-decide round 4 (N14)** | Drawn geometry is restored unconditionally, so a user resize while a transcript is pending is snapped back. The contract gives `fit` no way to tell its own growth from a user edit. |
| `stop()` truncates the last utterance | **accepted, not tested — owner: main loop, re-decide round 4 (N14)** | Disarm closes the utterance at wall-clock `now()`, ahead of the last delivered sample, so the final clip of every take loses its 250 ms tail. Lens severity: minor. |
| Arm-window strokes | **accepted, not tested — owner: main loop, re-decide round 4 (N14)** | `armBody` switches to freedraw and renders armed before awaiting `capture.prepare()`/`start()`; strokes drawn in that window are raw ink. Milliseconds warm, seconds on a cold first arm. |
| "Subtitles by amara.org" | **met (round 3)** | Four entries added to `HALLUCINATION_BLOCKLIST` ("subtitles by amara.org", "subtitles by the amara.org community", "자막 by", "시청해 주셔서 감사합니다") and `isHallucination` now also matches when the whole normalised text equals an entry ignoring spaces (equality, not substring). Proof: `test/unit/hallucination.test.ts` — the round-2 KNOWN GAP `it.each` flipped to a positive over the joined phrases, plus "matches a whole normalised text that differs from an entry only in spacing" ("시청해주셔서 감사합니다" and "thankyou" drop; "자막 by 한효정이 만든 회의록" is kept) and the existing short-word survivors ("young"/"youth"/"your"/"payout"/"뉴스룸") still pass. This row is L9's exhibit: it sat a whole round under *accepted* with its own fix written in the note. |
| `status.utterances` / `orphans` reset per arm | **accepted, documented** | `contracts.ts` defines them "since arm"; `completed` and `maxPendingSeen` stay cumulative. Any future gate asserting a cumulative count across arms will read low. |
