# EVIDENCE — 0.1.0 voice-areas

Gate rows for rounds 2–3. Statuses read back from `test-results/last-run.txt` (**23 passed, retries: 0**,
`/health` answered `{ok:true, model:large-v3-turbo, warm:true}` before the run), the unit run (**109/109**, was
88), the two round-2 review lenses and the round-2/3 fixes. Every path below was confirmed present on disk at
memo time; the unit counts were re-run at memo time (`npm test` → `Tests 109 passed (109)`). Caveats live in
RETRO.local.md (L1–L12); a row's caveat is the product behaviour the gate did not reach, not a hedge.

Round-4 cold-run re-confirmation (2026-09-18, no files modified by the run): `npm run build` clean, `npm test`
**150/150** (12 files — was 109), `npm run e2e` **24/24** (was 23 — one row grew, none dropped), STT `/health`
checked before and after (`{"ok":true,"model":"large-v3-turbo","warm":true}`). All four round-4 gates (G9–G12,
below) reconfirmed rather than re-derived. The working tree itself was NOT clean at cold-run time — untracked/
modified files under `scripts/` (`deploy.sh`, `kiosk-reload.mjs`, `kiosk-restore.mjs`, a staged deletion of
`kiosk-clear.mjs`) predate this memo and are outside its scope; noted so a later round does not mistake them for
memo output.

Provenance note for N8: the round-3 working tree was swept into an unrelated commit (`cd5e1cb`, kiosk acoustic
probe) by another process mid-round. Nothing in the evidence below depends on that commit — the cited proof is
the on-disk tree plus the re-driven runs — but a round-3-only diff has to be split out of `cd5e1cb`.

| Gate | Proof required | Status | Proof path / note |
| --- | --- | --- | --- |
| G1 vertical slice | real WAV → real STT → text inside the recognised shape | **met** | `test-results/evidence/g1-vertical-slice.png`; jfk.wav from the arm, stroke → ellipse, bbox unchanged within 2 scene px, transcript bound to the container. Round-1 looping-fixture caveat gone (clip played once). |
| G2 parallelism | 3 strokes in one hold, 3 transcripts, no stroke waits | **met (scope changed)** | `evidence/g2-parallelism.png`; three containers non-empty, `orphans: 0`, 3 shapes coexist while still holding with an earlier one already committed. Round-1's `maxPendingSeen >= 2` is **no longer asserted**: an utterance is dispatched only once VAD silence closes it, so two in-flight requests need two utterances ending inside one STT round trip, which no real-speech fixture produces (measured `maxPendingSeen: 1`). Accepted change of meaning, not a regression. |
| G3 recognition | unit + in-bundle geometry cases | **met** | `test/unit/stroke.test.ts` 11/11; shipped-bundle repeat at `evidence/g3-recognition.png` (circle→ellipse, box→rectangle, straight→line, near-vertical→rectangle, tap→null). |
| G4 fit | container dims unchanged for short/long/Korean; line text along the line | **met** | `evidence/g4a-fit-direct.png` (no growth at 240x120 **and** at the hostile 120x80 with a long Korean sentence — round 1's open half), `g4b-line-horizontal.png`, `g4c-line-slanted.png`, `g4d-line-legibility.png` (120 px line: fontSize ≥ 14, width ≤ 122, wrapped, text above the line). Documented exception stands: a shape too small for the floor font size is grown visibly. Harness-bent (L4): G4d uses the three-utterance fixture because the VAD seeds its floor on ko-long.wav's first syllable. |
| G5 failure paths | dead STT → ⚠ + retry; silence → shape kept; delete-while-pending | **met** | `evidence/g5a-failure.png`, `g5a-failure-retry.png`, `g5b-silence.png`, `g5c-delete-pending.png`. Harness-bent (L4): G5c uses the three-utterance fixture so no earlier utterance becomes an orphan free text. |
| G6 continuity | vanilla localStorage keys survive boot/reload/commit | **met** | `evidence/g6-continuity.png`; seeded `excalidraw` / `excalidraw-state` survive boot, reload, a voice commit and a second reload; ids, text and binding intact. |
| G7 kiosk | **round 3 live since 2026-09-18 00:5x KST** (`scripts/deploy.sh`, launcher with mic auto-grant + CDP port). Probe `/tmp/kiosk-r3.png`: page loads, `toolbar-voice` present, `capture.mic = ok` at boot, inputs Default + TouchDevice Mono, STT /health ok from the whiteboard; an armed stroke over CDP → ellipse 300×160 placeholder → no utterance in the silent room → discarded to a solid shape, no page errors (`scripts/kiosk-probe.mjs --stroke`). Round-1 evidence (22:37) superseded. | met (kiosk path) — **real-speech round trip on the wall still untested: the panel's mic array records digital silence, see REF-kiosk-audio.local.md** |
| G7 kiosk, round 4 | **round 4 live since 2026-09-18 11:5x KST** (`index-nyUMqaEe.js`; deployed with the new CDP page-reload path, board idle 48 min, 788 elements before and after). The whiteboard had been switched to **X11** by the remote-access hardening session that morning (`--ozone-platform-hint=auto` → x11; `maxTouchPoints` 40, PointerEvent yes, legacy `ontouchstart` absent — the founder's finger pinch-zoom and drawing work). Probe `/tmp/r4-kiosk.png` (`scripts/kiosk-probe.mjs --stroke`): `capture.mic = ok`, STT warm from the kiosk, one silent F9 stroke → marker + placeholder → disarm sweep → **nothing left** (live 788 = localStorage 788, 0 markers, 0 placeholders), no gear, glyph fill element present, `document.fonts.check("20px Excalifont")` true after `warmFonts`. | met (kiosk path). **Real speech on X11 with round 4 untested by us** — the founder's own morning test (round 3, Wayland) passed per the hardening session. |
| Board incident 2026-09-18 11:01 KST | The round-3 probe's cleanup (`kiosk-clear.mjs`) ran against the founder's LIVE board and flagged 785 elements deleted; restored at 11:04 by `scripts/kiosk-restore.mjs` (the page still held them as `isDeleted`; a stroke the founder drew in between was kept; localStorage 786 after save). Chromium profile storage snapshot taken first: `/root/dev_workspaces/wb-recovery/20260918-020217/`. `kiosk-clear.mjs` deleted from the repo; CLAUDE.md "Never" row added. | **resolved — nothing lost**; rule: probes never mutate the board scene-wide |
| G8 docs | CLAUDE.md + README cold-read verdict | **cold read: minor-gaps** | Reader reconstructed purpose, deploy, test and STT topology correctly. Four stalls, all additive: mic-button location has no visual description; no deploy-failure/rollback path for the single physical panel; unclear whether `npm run e2e` exit code is trustworthy when STT is down; stroke.ts geometry thresholds restated as fact in README with no pointer to the source of truth. |
| N2 boundary-race | zero-gap pair, disarm in window, undo while armed | **met (with an open sibling defect)** | `evidence/n2a-zero-gap.png`, `n2b-disarm-in-window.png`, `n2c-undo-while-armed.png`. Harness-bent (L4): N2a pins `preRollMs: 0`; at the default the assignment rule gives both sentences to the second stroke, and the only shape that can hold the first is a stroke spanning the deadline — which is the defect in the N2e row below. |
| N2d undo-before-speech | undone shape must not swallow the transcript | **met** | `test/e2e/voice.spec.ts:897`, `evidence/n2d-undo-before-speech.png`. Fails on the pre-fix `dispatch()` (transcript vanishes silently); passes with `liveTarget()`/`forgetStroke` — `orphans: 1`, `completed: 1`, `containerId: null`. |
| N2e pre-roll across a live stroke | default preRoll, speech then a slow stroke → orphans 0 | **unmet — open defect** | Measured on the real surface: utterance onset 639 ms (deadline 2139), pointer-down 1059, pointer-up 3025 → `orphans: 1`, "회의 안건 정리" placed as free text instead of in the container being drawn for it. Cause: a stroke enters `session.strokes` only inside the pointer-up conversion (`src/controller.ts:727`), so assignment finalises against a list missing the in-progress stroke. Fix shape: treat a live `currentStroke` like `Session.conversions` — provisional record or suppressed finality, re-run on push. Deliberately not asserted in the green suite. |
| N3 visible failure | denied/missing mic refuses to arm and says so | **met** | `evidence/n3-mic-denied.png`; mode stays `idle`, `recording: false`, typed `lastError`, `voice-tool--mic-missing`, activeTool unchanged, a later stroke produces no container. Deviation: headless Chromium rejects an ungranted `getUserMedia` with `NotSupportedError`, so the case asserts MicState ∈ {denied, missing, error}. |
| N5 zoom invariance | same physical gesture recognises the same at 0.5 / 1 / 2 | **met** | `evidence/n5-zoom-0.5.png`, `n5-zoom-2.png`, `n5-zoom.png`; 300x160 screen px → 600x320 scene px at 0.5 and 150x80 at 2, x within 2 scene px; a 6-screen-px tap creates nothing at either zoom. Closes round-1 L4. |
| N6 utterance assignment by words | three-utterance fixture, words per shape, both rhythms + palm tap | **met** | `evidence/n6a-draw-then-speak.png`, `n6b-speak-then-draw.png`, `n6c-palm-tap.png`; each shape holds its own words and is asserted NOT to hold its neighbours'; `orphans: 0`; the 3 px tap left no ink. Harness-bent (L4): `warmMicOnBoot: false` forced in every case, so the whole suite runs a cold mic the kiosk never has. |
| N7 single contract | src/ imports every contract it contains | **met** | `src/audio.ts` deleted, the retired recorder block removed from `contracts.ts`, `contracts-capture.ts` live and imported; `npm run build` green, no unreferenced module in `src/`. |
| N8 evidence integrity | retries 0, log at the cited path, every path exists | **met** | `playwright.config.ts:17` `retries: 0`; `outputDir: test-results/artifacts` so the start-of-run wipe spares the log; `test-results/last-run.txt` ends `23 passed (2.8m)` after the round-3 re-drive (was 2.7m in round 2); all cited evidence paths verified present at memo time, and `npm test` re-run to confirm 109/109. Closes round-1 L6. See the provenance note at the top about commit `cd5e1cb`. |
| R3 ghost sweep | a placeholder left by a reload is swept back to a plain shape | **met** | `evidence/r3-ghost-sweep.png`; a dashed rectangle with a bound "··" seeded into the vanilla `excalidraw` key returns solid, unbound, geometry unchanged, the "··" text gone. Unit-gated too: `test/unit/persist.test.ts` (12 cases). |

## Post-review fix gates (new unit rows — RETRO L5)

| Gate | Proof required | Status | Proof path / note |
| --- | --- | --- | --- |
| U1 supersede predicate | a target with no parts and an open utterance is not discarded | **met** | `test/unit/controller.test.ts` — the lens's exact case, a 60-combination property test asserting `isSuperseded === (assignUtterance picks the later stroke)`, and a full-controller case. Reverting the operator fails 3. This is the blocker the 22/22 suite could not see (RETRO L1). |
| U2 mic recovery after a latched failure | mute→error→prepare returns to ok without a reload | **met (node fake only)** | `test/unit/capture.test.ts`; `prepare()`'s fast path now gated on `mic === "ok"`, an intact graph re-validated or rebuilt. Fails on the pre-fix line. No browser or kiosk evidence — see U5. |
| U3 slow AudioContext resume | a context resuming at 900 ms stays ok, no spurious events | **met (node fake only)** | `test/unit/capture.test.ts`; one-shot 500 ms verdict replaced by a 250 ms poll up to 3 s. A context that never resumes still errors once with the same detail. |
| U4 STT upload filename | a wav blob uploads as .wav | **met** | `test/unit/stt.test.ts` stubs fetch and reads the FormData file name; wav/ogg/mp3/m4a mapped, else webm. Fails on the pre-fix branch (every round-2 upload was a WAV named .webm). |
| U5 hallucination short-entry | real short words survive the filter | **met** | `test/unit/hallucination.test.ts`; substring arm restricted to blocklist entries ≥ 8 chars — "young/youth/your/yours/payout/뉴스룸/속보 뉴스" survive (7 assertions fail on the pre-fix predicate) while every blocklist entry and "um thank you" still drop. |

## Round-3 gate rows (N10, N11, N12 — closed; RETRO "Closed / Retired by round 3")

| Gate | Proof required | Status | Proof path / note |
| --- | --- | --- | --- |
| N10 Status rendering | every `VoiceStatus` field is rendered and asserted, or carries a debug-only row | **met** | A drop now renders at the moment it happens: `controller.ts` raises `api.setToast({message, duration: 2500})` — `Filtered: "<text>"` for a blocklist/non-empty drop, `No speech heard for that shape` when a non-orphan target is discarded with no text — and `buttonTitle(status)` in `toolbar.tsx` adds `dropped N: "<last>"` to the mic button's tooltip. Proof: `test/unit/controller.test.ts` → describe "a dropped transcript is rendered, not only counted (gate N10)" → "toasts the filtered text so a blocklist hit cannot be mistaken for a silent room" + "toasts a shape that ends with no text at all" (both assert the 2500 ms duration); `test/unit/toolbar.test.ts` → describe "buttonTitle — the toolbar says how many transcripts were thrown away" → "says nothing about drops while nothing has been dropped", "names the count and the text that was filtered", "keeps the failure line as well: two different channels, both readable"; real surface `test/e2e/voice.spec.ts:462` (G5b) asserts `.Toast .Toast__message` reads "No speech heard for that shape", logged green at `test-results/last-run.txt:12` (test 9, 3.7s). Residue (accepted, owner: main loop, re-decide round 4): no e2e drives `status.dropped > 0` — the silence fixture never returns a transcript, so only a real hallucination could raise the counter; the tooltip half is gated by the pure `buttonTitle` instead, which is the N9 shape. |
| N11 Recovery | every blocking module state is entered, cleared, and the product works again without a reload | **met (node fake; kiosk still owes N13)** | Proof: `test/unit/capture.test.ts` → describe "N11(a) — an audio context that stays suspended past the window and then comes back" → "goes error, then ok, and arms again without a reload" (context suspended past the whole 3 s window → mic `error`; HAL returns → `stop()`/`start()` gives mic `ok`, `active: true`, `rig.calls === 1`, i.e. no re-acquire and no reload); describe "N11(b) — a muted track" → "errors on mute, clears on unmute, and starts again" (typed detail on the error, `start()` works on the same stream); the two `prepare()` cases renamed "N11(c): re-checks an intact graph instead of returning the latched error state" and "N11(c): tears the cached graph down and rebuilds when the context is no longer running"; plus the two round-2 slow-resume cases now titled N11(a). Every case names its gate in its title, so the gate is greppable from the suite. Caveat: node fake HAL only — the real audio graph is still N13. |
| N12 Scale agreement | the level meter and the VAD threshold marker are asserted to share one scale | **met** | `LEVEL_GAIN` (×4) deleted: `capture.onLevel` and the new `capture.noiseFloor` emit RAW RMS, with unit **and** scale stated in `src/contracts-capture.ts` (0..1, speech ~0.02–0.2); `src/level.ts` is the one display mapping (`meterPercent` at full scale 0.06, `effectiveThreshold(setting, 3× floor)`, `meterScale(rms, threshold, floor) → {bar, mark, threshold}`); `settings-panel.tsx` draws bar and marker from `meterScale`, `toolbar.tsx` routes `status.level` through `meterPercent` into the `--voice-level` CSS var, `vad.ts` exports `FLOOR_MULTIPLIER` and a readonly `noiseFloor`. Proof: `test/unit/level.test.ts` → describe "meterScale — bar and marker are the same function of the same unit" → "puts the marker exactly where the bar ends when the room sits on the threshold", "orders bar against marker the way the VAD orders level against threshold", "draws the marker at the threshold the VAD really uses, not at the setting alone", "agrees with vad.ts on where the line is, for the same floor" (property case over a live `createVad` across 5 room floors), "clamps into the meter instead of overflowing it"; `test/unit/capture.test.ts` → describe "N12 — the number the meter is drawn from" → "emits RAW RMS through onLevel, not a display-gained copy of it" + "reports the VAD's noise floor on that same scale". Before/after reproduced: re-inserting the ×4 fails the two capture cases (0.4 vs 0.1). Accepted (owner: main loop, re-decide round 4): the toolbar has no meter surface, so its half of the agreement is the CSS var + recording-dot halo, not a bar a test can read. |
| U4bis STT upload filename | a WAV blob uploads as `.wav` | **met (already covered)** | `test/unit/stt.test.ts` → "names the round-2 PCM blob segment.wav" asserts the multipart file name for an `audio/wav` blob plus the ogg/webm fallbacks. Re-run at memo time; no new test was needed. |

## Round-4 gate rows (4a regions, 4b glyph/menu/languages, 4c review fixes)

Run of record for 4c: `npm test` 150 passed / 12 files; `npm run build` clean; `npm run e2e` 24/24 with
`curl -s http://100.81.33.83:8770/health` → `{"ok":true,"model":"large-v3-turbo","device":"cuda","warm":true}`
checked before AND after the run; log `test-results/last-run.txt`, screenshots `test-results/evidence/`.

| Gate | Proof required | Status | Proof path / note |
| --- | --- | --- | --- |
| 4a-R1 A drawn shape is a REGION, not a drawing (founder request 1) | the shape disappears when the words land; the text is fitted into its bounding box at the largest size that fits | **met** | Real surface: `test/e2e/voice.spec.ts` G1 (`evidence/g1-region-pending.png` → dashed rectangle equal to the stroke's bbox ±2 px with an animated "·"; `evidence/g1-vertical-slice.png` → `markers().length === 0`, `els.length === 1`, the text inside the region, `containerId === null`, `autoResize === false`, and the maximality check "one step more ceiling changes nothing"); G4a for hostile 240x120 / 120x80 regions in both languages; "native tool modifier" for a rectangle drawn with the native tool. Unit: `test/unit/fit.test.ts`. |
| 4c-R1 A region is deleted by a commit or by the disarm — NEVER mid-take | a region drawn before the first spoken label survives while latched, and the disarm removes leftovers in one update with a counted toast | **met** | `test/unit/controller.test.ts` → describe "a region the founder drew and never spoke into" → "survives on the canvas while the tool stays latched, and only the disarm removes it" (stroke A, stroke B, one late label that `assign.ts` gives to B: A's marker is still `isDeleted: false` after B commits, no toast; the disarm then deletes it and toasts once) + "sweeps several at once with a toast that counts them" (three unspoken regions → one update, `"3 regions removed — nothing was said"`). Round 4a deleted A the moment B's transcript landed, i.e. every box drawn before the first label while latched. Real surface unchanged: G5b still asserts the single-region toast and `els.length === 0`. |
| 4c-R2 N2e — a stroke still under the pen when the pre-roll deadline expires | `orphans === 0` and the words land in that stroke's region; the region is not deleted | **met** | `test/unit/controller.test.ts` → describe "gate N2e — a stroke that is still under the pen when the deadline passes" → "waits for the pen, then writes the words into the region being drawn". Fix: `runAssignment` treats `currentStroke?.session === owner` like a queued conversion (the flush barrier now covers the consumer, RETRO L3), and `convertStroke`'s `finally` re-runs it. Before/after reproduced: restoring the old guard fails the case with `orphans: 1` and the marker deleted under a "No speech heard" toast. Not reachable from the e2e — the slowest stroke there is `stepMs: 14` ≈ 0.4 s of pen-down against a 1.5 s pre-roll — which is why N2e stayed open for two rounds. |
| 4c-R3 Landed words are never overwritten | a failed take into a region whose words already landed changes nothing on the canvas | **met** | `test/unit/fit.test.ts` → "never writes over words that already landed" (`markFailed` returns `[]`); `test/unit/controller.test.ts` → "leaves an orphan's transcript alone when a later take into it fails" (first utterance commits, second throws in `transcribe`: the text still reads the transcript and `status.failed === 1` carries the news). Before: the ⚠ overwrote the committed text, whose only other copy was `entry.parts` in memory — a reload destroyed the founder's words for good. |
| 4c-R4 Failure warnings are swept by a stamp, not by their text | an unbound "⚠ STT" the app wrote is removed by a reload; a founder-typed one is not | **met** | `src/fit.ts` stamps `customData.voiceFailed` (`VOICE_FAILED_CUSTOM_DATA`) on every warning and a commit clears it; `src/persist.ts` sweeps unbound texts that are placeholder dots **or** stamped. Proof: `test/unit/persist.test.ts` → "deletes a free-standing warning the app STAMPED (a failed line region, or a marker-less retry)" + the existing "leaves real text, real shapes and free-standing STT warnings untouched" (unstamped); `test/unit/fit.test.ts` → "stamps the warning so a reload can sweep it, bound or not" + "clears the stamp again when a retry finally lands the words". |
| 4c-R5 One Ctrl+Z after a commit does not resurrect the scaffolding | the marker's deletion is not part of the undoable commit step | **met (unit + reasoning; the undo surface itself is the library's)** | `controller.ts` `recommit` now applies the words with `IMMEDIATELY` and the marker's deletion with `NEVER`, so the store's delta for the commit contains no marker state to restore. Residue (accepted, owner: main loop, re-decide round 5): undoing twice still leaves the placeholder "·" the commit reused (same element id — that IS the founder's undo history), and `persist.sweepGhostPlaceholders` removes it at the next reload. |
| 4b-R1 The mic glyph reacts to the volume (founder request 2) | the capsule/ring track the level and the accent marks "recognised", readable at 2–3 m | **met** | `test/e2e/voice.spec.ts` "toolbar latch" → `heard.peakLevel > 0`, `heard.sawSpeaking === true`, and (round 4c) at least three DISTINCT partial levels, which is what tells "reacts to volume" from the strobe the 0.06 axis produced. `test/unit/level.test.ts` → describe "glyphLevel — the mic glyph's own display curve" (0 at silence, 1 only at its own 0.25 full scale, monotone, conversational speech mid-scale while `meterPercent` is already pinned at 100). `test/unit/toolbar.test.ts` → "fills through level.ts's GLYPH mapping, not the VAD meter's axis". Wall legibility: the glyph SVG is forced to 22 px (the library's own rule pinned it to 16), the ring is `box-sizing: border-box` so the halo is centred, and while speaking the accent is floored (`opacity ≥ 0.7`, fill `≥ 0.45`) so "recognised" does not need volume to be seen. |
| 4b-R2 Settings live in the main menu, no top-right gear (founder request 3) | the gear is gone, the vanilla menu is intact, the panel opens from the menu and is usable | **met** | `test/e2e/voice.spec.ts` "settings live in the main menu" → `voice-settings-gear` count 0, every `DefaultMainMenu` testid present plus `menu-voice-settings`, the panel visible (`evidence/round4b-menu-settings.png`, `round4b-main-menu.png`). Round 4c added the usability half: the panel renders OUTSIDE `.excalidraw`, so the same test now asserts the level bar's fill and the action buttons have a non-transparent background and the inputs a `solid 1px` border (they resolved to nothing, i.e. invisible controls on a console-less wall panel), and that a tap outside or Escape closes the panel while drawing nothing on the board. |
| 4b-R3 Korean and English only (founder request 4) | ja/zh cannot be selected, stored or sent, and the server enforces the same list | **met** | `test/e2e/voice.spec.ts` "settings live in the main menu" asserts the select's options are exactly `["", "ko", "en"]`; `test/unit/settings.test.ts` coerces a stored `ja`/`zh` back to auto; the STT server's `STT_LANGUAGES` allow-list answers `language=ja` with 400 before reading the body and ranks auto-detection over the same list. Residue (accepted, owner: main loop, re-decide round 5): the two lists are independent sources of truth — `/health` does not report `STT_LANGUAGES`, so a server-side change diverges silently. |
| 4c-R6 Fitting waits for the fonts | no take is fitted against fallback metrics, and no gate assumes a precondition production does not provide | **met** | `fit.warmFonts()` (measure once, then `document.fonts.ready`, cached) is awaited by `App` at boot and by `armBody` before the mic is opened; the two fit gates that used to warm the font *inside the page* now call `voice.fit.warmFonts()`, i.e. the app's own path, and the test-private `document.fonts.ready` lines are gone (`test/e2e/voice.spec.ts` G1 `refit`, G4a). |

## Round-4 gate index (G9–G12, DESIGN.local.md's numbering — cross-referenced, not duplicated)

The four founder requests each got a cycle-level gate number continuing G1–G8; the detailed proof for each already
lives in the 4a/4b/4c rows above (round-4 built in three passes, not four gates), so this table points at that
proof rather than re-stating it.

| Gate | Founder request | Status | Proof (see row above) |
| --- | --- | --- | --- |
| G9 region lifecycle | 1 — shapes are region selection only, not drawings | **met** | 4a-R1, 4c-R1, 4c-R3, 4c-R4, 4c-R5 |
| G10 mic glyph | 2 — mic icon animates with the audio volume | **met** | 4b-R1 |
| G11 settings location | 3 — settings move into the top-left main menu, gear removed | **met** | 4b-R2 |
| G12 language allow-list | 4 — ja/zh removed, client + server | **met** | 4b-R3 |

## Open rows — defects and untested risks (RETRO L5: every risk exits as a row)

| Item | Status | Note |
| --- | --- | --- |
| Level scale mismatch | **met (N12, round 3)** | See the N12 row below. |
| `dropped` / `lastDropped` not rendered | **met (N10, round 3)** | See the N10 row below. |
| Capture path on real hardware | **untested — gate N13, owner: main loop** | Worklet path, ScriptProcessor fallback, resume-after-gesture, clock-offset drift, WAV cut accuracy: offline arithmetic plus Chromium's fake device only. Cold-start suspend probe on the panel unrun (RETRO L8, gate N13). |
| `utteranceSession` never deleted from | **unmet — owner: main loop** | Every utterance the page hears keeps a strong reference to its Session, so `sessions.delete(owner)` frees nothing on a kiosk that never reloads (RETRO L6). Round 3 closed the sibling case for `FailedEntry` with `pruneFailed()`; copy that shape — name the deleter and prove the entry is gone after its key dies. Re-labelled from *untested* per L9: a map with a known missing deleter is a defect, not an unknown. |
| `FailedEntry` for a deleted shape | **met (round 3)** | `pruneFailed()` drops every failed entry whose container/text is gone from the scene (`findPair` miss) and forgets its stroke record; called at the top of `resolveTargets` and first in `retryFailed`, so the failed count, the lit retry button and the retained WAVs cannot grow. Proof: `test/unit/controller.test.ts` → "a failed entry whose shape has left the scene is pruned" (undo-then-disarm clears `failed` to 0; an attempt-exhausted entry that `retryFailed` used to skip forever is dropped on the next retry with no request sent). Both cases fail on the pre-fix controller. |
| Multi-touch second contact | **accepted, not tested — owner: main loop, re-decide round 4 (N14)** | A second pointer-down before the first pointer-up overwrites `currentStroke`; the first contact's ink is never converted. IR frame behaviour unknown until the kiosk pass. |
| `fit.discard` / `markFailed` restore | **retired by round 4a — the described code no longer exists** | There is no restore path any more: a drawn shape is a region MARKER, so `discard` deletes it and a commit deletes it. What replaces this row: **region geometry is frozen at stroke time** (`VoiceTarget.shape`, set once in `controller.ts` at conversion), so moving or resizing a *pending* marker leaves the committed words at the original spot. Deliberate — the marker is usually gone by the second utterance, which is the case the freeze exists for. **Accepted, not tested — owner: main loop, re-decide round 5.** A fix would translate by `marker.x - shape.x` while the marker is still alive. |
| `stop()` truncates the last utterance | **accepted, not tested — owner: main loop, re-decide round 4 (N14)** | Disarm closes the utterance at wall-clock `now()`, ahead of the last delivered sample, so the final clip of every take loses its 250 ms tail. Lens severity: minor. |
| Arm-window strokes | **accepted, not tested — owner: main loop, re-decide round 4 (N14)** | `armBody` switches to freedraw and renders armed before awaiting `capture.prepare()`/`start()`; strokes drawn in that window are raw ink. Milliseconds warm, seconds on a cold first arm. |
| "Subtitles by amara.org" | **met (round 3)** | Four entries added to `HALLUCINATION_BLOCKLIST` ("subtitles by amara.org", "subtitles by the amara.org community", "자막 by", "시청해 주셔서 감사합니다") and `isHallucination` now also matches when the whole normalised text equals an entry ignoring spaces (equality, not substring). Proof: `test/unit/hallucination.test.ts` — the round-2 KNOWN GAP `it.each` flipped to a positive over the joined phrases, plus "matches a whole normalised text that differs from an entry only in spacing" ("시청해주셔서 감사합니다" and "thankyou" drop; "자막 by 한효정이 만든 회의록" is kept) and the existing short-word survivors ("young"/"youth"/"your"/"payout"/"뉴스룸") still pass. This row is L9's exhibit: it sat a whole round under *accepted* with its own fix written in the note. |
| Native multi-point line while armed | **unmet — owner: main loop** | `props.onPointerUp` fires at the end of EVERY pointer interaction, including the first click of a multi-point native line, so that first segment is converted into a stamped marker (with `target.shape` frozen to it) while the founder is still clicking the rest. Round 4c did not fix it: bailing on `appState.multiElement` risks never converting the line at all, because the finalising double-click's pointer-up may still carry it. Needs the "convert when the element is finalised" path plus an e2e row for the native line tool (today only native RECTANGLE is gated). |
| Region geometry frozen at stroke time | **accepted, not tested — owner: main loop, re-decide round 5** | See the retired `fit.discard` row above: moving or resizing a marker while its transcript is in flight leaves the words at the original spot. |
| Client/server language allow-lists diverge | **unmet — owner: main loop** | `src/settings.ts` `ALLOWED_LANGUAGES` and the server's `STT_LANGUAGES` are two hard-coded sources of truth with nothing comparing them; `/health` does not report the server's list. Cheapest fix: return `languages` from `/health` and have `scripts/smoke.mjs` fail on a mismatch. Deliberately not done in 4c: it needs an STT-server redeploy, which is a separate blast radius from a UI round. |
| VAD noise floor seeded from speech | **accepted, not tested — owner: main loop, re-decide round 5** | If speech is already running when the stream opens, the initial floor is the mean of voiced frames and the effective threshold (3x floor) climbs above the level: the glyph fills to 100% while never turning green until the floor decays (~4.7 s measured with `ko-long.wav` and `warmMicOnBoot` off). Mitigated on the wall by `warmMicOnBoot` defaulting true. A fix seeds the floor from a percentile/minimum of the first N frames — pure `vad.ts` change, so it wants its own unit gate rather than a drive-by edit. |
| `status.utterances` / `orphans` reset per arm | **accepted, documented** | `contracts.ts` defines them "since arm"; `completed` and `maxPendingSeen` stay cumulative. Any future gate asserting a cumulative count across arms will read low. |

## Round 5 — transcription concurrent with region selection (2026-09-18)

Run state at memo time: `npm run build` clean (`tsc --noEmit` + vite), `npm test` **165/165** (12 files, was 150),
`npm run e2e` **27/27** (was 24), `retries: 0`, log at `test-results/last-run.txt` (ends `27 passed (3.1m)`).
`curl -sf http://100.81.33.83:8770/health` answered `{"ok":true,"model":"large-v3-turbo","warm":true,"skipped":1}`
immediately before the run — the `skipped` field is new in this round's server build, so its presence is itself
proof the deployed server is the one this round wrote.

| Gate | Proof required | Status | Proof path / note |
| --- | --- | --- | --- |
| G13 words before pen-up | real surface: pen-up → committed text ≤ 400 ms, with the STT round trip from the same run | **met** | `test/e2e/voice.spec.ts` "R5a…", `evidence/r5a-lands-when-the-region-exists.png`. Three runs: **78 / 80 / 93 ms** against a round trip of **1583 / 1629 / 1636 ms**. Both timestamps are taken INSIDE the page (`helpers.watchWords`: a capture-phase `pointerup` listener plus a 10 ms scene sampler), because a CDP round trip per sample is the same order as the number measured. The gate also asserts `completed === 0` at the moment the pen is lifted, so the 80 ms cannot be a commit that had already happened. |
| G13 before/after | the same script on the round-4 build | **met** | `git worktree add /tmp/xv-head 17a3a1d`, built there, served on `:4174`, the identical spec run against both: **round 4 = 1874 ms, round 5 = 85 ms** (and 80 ms for the gate's own polling variant). The worktree and its spec were removed after the measurement; reproduce with the recipe in this row. |
| G14 interim preview | a dimmed, stamped, non-placeholder text while `status.speaking` is still true and before `endMs` | **met** | "R5b…", `evidence/r5b-interim-while-speaking.png`, `r5b-final-replaces-interim.png`. Observed: preview `"이번 주 목표는"` at capture clock 4897 ms, `speaking: true`, `opacity: 45`, utterance onset ~1715 and `endMs` ~7155 — i.e. words on the wall **~4 s before the sentence finished**. The final commit is asserted to be the SAME element id with the stamp cleared and zero markers left. |
| G15 provisional correction | preview in region A, final assignment to region B, A handed back | **met — harness-bent** | "R5c…", `evidence/r5c-preview-in-region-a.png`, `r5c-region-a-reverted.png`. A ends as `"··"` with its marker alive and no stamp; B holds the sentence; the disarm then sweeps A as "nothing was said". **Bent parameters, named in the test body:** `interimMs: 400`, `preRollMs: 6000`. At the shipped defaults the case is unreachable — the pre-roll window closes 1.5 s after the onset and the measured 1.6 s round trip cannot deliver a preview inside it — which is also the honest scope of the revert path: at defaults the provisional region is almost always the final one. |
| G16 abandoned request | an aborted client request costs no GPU time, visibly | **met** | `scripts/stt-abort-check.mjs` (real Chromium fetch, aborted while queued) → `PASS`, `/health.skipped` 0 → 1, server log `skipped abandoned request after 0.39s queued (1 so far)` immediately after the blocking take's own completion line (`ssh stt-desktop … Get-Content C:/Users/jeeni/stt/server.log -Tail 8`). Timing matters and is documented in the script: a short blocker, or a request aborted before uvicorn has read it, never reaches the lock at all (that path is *also* free, but it proves nothing about the check). |
| G12 language allow-list, re-proved after the redeploy | ko auto → ko; `language=ja` → 400 | **met** | `ko-short.wav` with no language → `{"text":"회의 안건 정리","language":"ko"}`; `language=ja` → HTTP 400 `language ja not enabled; allowed: ko,en`; `language=ko` → `{"text":"회의 안건 정리"}`. Run against the round-5 server build. |
| Server deploy | back up, scp, restart, warm, prove | **met, with an incident** | `server.py.bak3` (7893 bytes) taken before the first copy. The first restart left the server **down**: `schtasks /End` + `/Run` raced the old process's socket, uvicorn logged `[Errno 10048]` and called `sys.exit(1)` — which the `except OSError:` retry loop could never catch, so the task simply exited (log at 13:05). Recovered by a second `/Run`, then fixed in the code (`except (OSError, SystemExit)`, 12 bounded attempts) and redeployed. Total outage ~10 minutes, on a dev-only service, with no QA running. |
| Unit gates | the seams no browser can reach | **met** | `test/unit/controller.test.ts`: transcription dispatched at utterance end with the pen down (`transcribe` called before pointer-up, `pending: 1`, `completed: 0`); `settle` idempotent and order-independent (assignment-first case, then a disarm that calls it again — `completed` stays 1, one text, one request); a failure that arrives before the assignment lands as ⚠ on the final region and the retry recovers it; interim slices aborted at utterance end; a slice answering after the final transcript is ignored; the provisional region reverted to its placeholder. Plus the G5c/N2d discriminator pair (`liveTargets`). `test/unit/fit.test.ts`: `commitInterim` fits like a commit, keeps the marker, dims and stamps; `resetPlaceholder` restores the dot, the binding and the size; the line-region variants. `test/unit/persist.test.ts`: an interim preview and its marker are swept on reload, bound or free. `test/unit/settings.test.ts`: `interimMs` clamped. |

### Round 5b (review fixes, 2026-09-18)

`npm run build` clean, `npm test` **172/172** (12 files, was 165), `npm run e2e` **27/27**, `retries: 0`
(`test-results/last-run.txt`, ends `27 passed (3.2m)`), with `curl -s http://100.81.33.83:8770/health` →
`{"ok":true,"model":"large-v3-turbo","warm":true,"skipped":1}` taken immediately before it. The run printed
`[R5a] pen-up -> words: 80 ms | STT round trip: 1583 ms`, so the per-utterance barrier did not cost the round's
headline number.

**One flake, named:** the e2e was run three times on this tree (a flake is a failure, RETRO N8). Runs 1 and 3 were
27/27; run 2 failed only `toolbar latch: tapping the voice tool arms and disarms it, and the glyph shows what the mic
hears`, which then passed **3× in isolation** (`npx playwright test -g "toolbar latch"`, 6.9–7.0 s each). Its trace
was lost because a later run wiped `test-results/artifacts`, so the failing assertion was NOT captured — the honest
status of that gate on this tree is "green twice, unexplained red once". It asserts a level/`speaking` transition
driven by the fake mic's timing and touches nothing round 5b changed; it is the row a round-6 pass should re-run
with the trace kept.

| Gate | Proof required | Status | Proof path / note |
| --- | --- | --- | --- |
| G17 failure after a preview | a take that fails after its preview landed reports it in the region | **met (unit, both layers)** | `test/unit/fit.test.ts` "DOES write over an interim preview…" and `test/unit/controller.test.ts` "writes the ⚠ over the preview…": ⚠ STT, `voiceFailed` stamped, `voiceInterim` cleared, opacity 100, marker still dashed. Each was run against the pre-fix code and fails there (`expected 'Ship the' to be '⚠ STT'`). The harness's fake `markFailed` now carries the real `hasLandedTranscript` guard — its absence is the single reason the shipped 165-test suite could not see this. |
| G18 erased mid-preview | a region erased while its own preview is up drops silently | **met (unit)** | `test/unit/controller.test.ts` "drops the words silently even though the preview's re-render pruned the entry" — `orphans 0`, `completed 0`, no text on the canvas. Fails on the pre-fix guard (`expected 1 to be +0`). |
| G19 region created after the audio was sent | erasing it still drops silently (speak-while-drawing) | **met (unit)** | "drops a region created AFTER the audio was sent and erased before the answer came back". Fails with `noteLiveTarget` removed (`expected 1 to be +0`). |
| G20 barrier is per utterance | an utterance no open stroke could claim settles while that stroke is open | **met (unit)** | "settles an utterance no open stroke could claim…" (pen-down at onset+1800 against a 1500 ms pre-roll: `completed 1` with no pen-up) plus the companion "still waits for a stroke whose pointer-down IS inside the window (gate N2e)". The first fails with the session-wide barrier restored (`condition never became true`); N2e itself and the e2e R5a/N6b gates cover the other side. |
| G21 no undo step for an unchanged re-render | a preview leaving a committed region writes the words with NEVER | **met (unit)** | "re-renders it as cosmetic churn…": exactly one `IMMEDIATELY` write carrying the transcript; two with the fix reverted. The fake API now records each write's `captureUpdate`. |
| WAV release | a resolved take stops holding its audio | **accepted, not directly gated — owner: main loop, re-decide round 6** | `settle`'s `finish()` drops `u.stt.blob` and the `utteranceSession` entry; nothing on the surface can observe a freed Blob, so what is gated is that the RETRY path still works after settle (`test/unit/controller.test.ts` "a failure that arrives before the assignment lands as ⚠ STT on the final region, and retries", which now retries from the `failed` map's own reference). A kiosk-side memory measurement was not taken. |

### Open rows added by round 5

| Item | Status | Note |
| --- | --- | --- |
| Interim cadence is round-trip-bound | **accepted, documented — owner: main loop, re-decide round 6** | A preview costs a full round trip (~1.6 s measured), and the next slice only starts after it, so the effective refresh is `interimMs + latency` (~2.8 s at the default). Words therefore appear in chunks, not word by word. True incremental behaviour needs a streaming endpoint, which is out of scope for a repo that talks to an OpenAI-compatible `/v1/audio/transcriptions`. |
| `status.pending` excludes interim slices | **accepted, documented** | Deliberate: `pending` is what the badge counts and what the e2e's `settled()` waits on, and interim work is cosmetic. The server-side sink for abandoned slices is `/health.skipped`; there is no client-side counter for slices sent. |
| The suite's fixture clock | **unmet — owner: main loop** | `helpers.launchWithClip` documents "the clip starts at the arm", and that is right — but the VAD's floor seeding (open row above) means a clip whose speech starts at 120 ms loses its first seconds anyway (measured: 5.4 s of `ko-long.wav` arriving as one 0.5 s utterance). Round 5 works around it with `helpers.ensureLeadInClip`, which prepends silence into git-ignored `test-results/fixtures/`. The real fix is the floor-seeding row (`vad.ts`, percentile of the first N frames), after which the lead-in helper should be deleted rather than kept. |
| Kiosk not re-probed for round 5 | **unmet — owner: main loop** | Nothing in this round was deployed to the whiteboard (`100.102.3.47`): the main loop owns deploys. The interim preview's legibility at wall distance (45 % opacity on a bright panel) is exactly the kind of thing the round-4 persona lens existed to catch and has NOT been looked at on the wall. |

### Open rows added by round 5b

| Item | Status | Note |
| --- | --- | --- |
| Interim previews are BOUND to the marker | **accepted, not tested — owner: main loop, re-decide round 6** | `fit.commitInterim` keeps `containerId` on the founder's marker and copies the probe's layout, so a transcript that does not fit even at `minFontSize` could in principle make the library grow the shape they drew, mid-take. Not reproduced: fit's unit environment fakes the layout and the R5b e2e used a region the sentence fitted in. The clean fix (preview as free text, marker's `boundElements` untouched) requires unbinding at preview time and rebinding in `resetPlaceholder` — a decision, not a patch, so round 5b left it alone rather than shipping the one-sided pair `commitInterim` already warns about. |
| Regions swept by the DISARM while a take is in flight | **accepted, documented** | `settle`'s erased-mid-take rule now also fires when the entry was pruned by the disarm's unspoken sweep rather than by the founder (a closed, empty region whose late utterance arrives afterwards): those words are dropped instead of orphaned. Consistent with the round-5 behaviour for the same case when the entry survived, and the sweep already toasts. Not exercised by a gate. |
